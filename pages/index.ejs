<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />

    <title><%= title %></title>
  </head>
  <body>
    <% if(created) {%>
    <div class="alert alert-success">Note created successfully!</div>
    <% } %>

    <div class="container">
      <h1><%= title %></h1>
      <p>This is a simple server created using Node.js and Express.js.</p>
      <form action="/" method="post" class="mb-2">
        <div class="form-group mb-2">
          <input type="text" class="form-control" name="title" required />
        </div>

        <button class="btn btn-primary">Create!</button>
      </form>

      <% if(notes.length) {%>
      <div class="card">
        <ul class="list-group">
          <% for(let i=0; i< notes.length; i++){%>
          <li
            class="list-group-item d-flex justify-content-between align-items-center"
          >
            <%= notes[i].title%>
            <div class="buttons">
            <button class="btn btn-primary" data-type="edit" data-title="<%= notes[i].title %>" data-id="<%= notes[i].id %>">Edit</button>
            <button class="btn btn-danger" data-type="remove" data-id="<%= notes[i].id %>">&times</button>
            </div>
          </li>
          <% }%>
        </ul>
      </div>
    
    <%} else { %>
      <p>No notes found.</p>
        <% } %>
     </div>
  </body>
  <script src="/app.js"></script>
  <!-- <script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
     integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
    crossorigin="anonymous"
  ></script>
   -->
</html>
